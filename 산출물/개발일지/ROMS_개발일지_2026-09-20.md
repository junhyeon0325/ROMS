# ROMS 개발일지 - 2026-09-20

> **최종 수정**: 2026-09-20 19:00

## Development Journal

### 19:00 Strict Mode 마운트 상태 복원

- **대상**: `app/admin/codes/hooks/useCodeGroupManagement.ts` (변경 line 24-27), `app/admin/codes/hooks/useDetailCodeManagement.ts` (변경 line 29-32)
- **내용**: 두 훅의 마운트 effect setup에서 `isMountedRef.current`를 true로 복원하고 cleanup에서는 false로 유지했다. Strict Mode의 setup→cleanup→setup 재실행 뒤에도 그룹·상세 코드 조회와 CRUD 후속 갱신이 마운트 상태 보호에 의해 건너뛰지 않는다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. 두 훅에서 setup true 및 cleanup false 배치를 확인했다. ESLint 설정이 없어 새 설정을 만들지 않고 실행하지 않았으며, 로그인 세션이 없어 브라우저 검증은 실행하지 않았다.

### 18:51 공통코드 조회 실패 알림 연결

- **대상**: `lib/context/AdminFeatureContexts.tsx` (변경 line 25-31, line 45-65), `app/admin/codes/hooks/useCodeGroupManagement.ts` (변경 line 6-46, line 53-84), `app/admin/codes/hooks/useDetailCodeManagement.ts` (변경 line 20-65, line 86-119), `app/admin/codes/components/DetailCodeSection.tsx` (변경 line 10, line 40-41, line 64-72)
- **내용**: Context 목록 조회의 실패 응답과 예외를 오류 상태로 노출하고, 코드 그룹 조회 오류는 전용 훅에서, 상세 코드 조회 오류는 기존 DetailCodeSection 알림에서 한 번만 표시하도록 연결했다. 상세 코드 훅에는 요청 식별자와 마운트 상태 보호를 추가해 이전 그룹의 늦은 응답과 언마운트 후 상태 변경을 막았다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. 오류 상태→알림 연결, 정상 빈 목록에서 오류 상태 미설정, 동일 Error 객체의 중복 알림 방지와 CRUD 알림 유지 경로를 코드로 확인했다. ESLint 설정이 없어 새 설정을 만들지 않고 실행하지 않았다.

### 18:30 상세 코드 테이블 표시 영역 분리

- **대상**: `app/admin/codes/components/DetailCodeTable.tsx` (추가 line 1-78), `app/admin/codes/components/DetailCodeSection.tsx` (변경 line 17-24, line 244-254)
- **내용**: 상세 코드 목록의 테이블 열, 빈 상태, 행 선택, 인라인 추가·수정 표시 JSX를 DetailCodeTable로 이동했다. 선택 코드, 선택 그룹, 편집 상태, 저장 핸들러를 props로 전달해 기존 검색·필터·CRUD와 그룹 변경 후 조회·초기화 흐름을 유지했다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. DetailCodeTable의 목록·선택·편집·저장 props 연결과 useDetailCodeManagement 호출 유지 여부를 확인했다. ESLint 설정이 없어 새 설정을 만들지 않고 실행하지 않았다.

### 18:22 상세 코드 관리 API 훅 분리

- **대상**: `app/admin/codes/hooks/useDetailCodeManagement.ts` (추가 line 1-109), `app/admin/codes/components/DetailCodeSection.tsx` (변경 line 10-26, line 40-41, line 56-60, line 195-202, line 227-232, line 246-251)
- **내용**: 선택된 그룹별 상세 코드 조회와 캐시 정리, 생성·수정·삭제 요청, 로딩·오류 상태 및 기존 성공·실패 알림을 전용 훅으로 이동했다. DetailCodeSection의 JSX, 검색·선택·인라인 편집 상태와 기존 검증·알림 메시지는 유지했다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. 선택 그룹 변경 시 훅의 조회 effect와 섹션의 UI 초기화 effect가 함께 실행되고 CRUD 핸들러가 훅의 저장·삭제 함수에 연결됨을 확인했다. ESLint 설정이 없어 새 설정을 만들지 않고 실행하지 않았다.

### 18:12 코드 그룹 테이블 표시 영역 분리

- **대상**: `app/admin/codes/components/CodeGroupTable.tsx` (추가 line 1-83), `app/admin/codes/components/CodeGroupSection.tsx` (변경 line 12-19, line 214-223)
- **내용**: 코드 그룹 목록의 테이블 열, 행 선택, 인라인 추가·수정 표시 JSX를 CodeGroupTable로 이동했다. 기존 편집 상태와 저장 이벤트, 선택 그룹 코드는 props로 전달해 헤더의 수정·삭제 동작과 DetailCodeSection 연동을 유지했다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. props 연결에서 `selectedGroupCode`, `groupEdit`, 선택 및 저장 핸들러가 모두 전달됨을 확인했다. ESLint 설정이 없어 새 설정을 만들지 않고 실행하지 않았다.

### 17:53 코드 그룹 관리 API 훅 분리

- **대상**: `app/admin/codes/hooks/useCodeGroupManagement.ts` (추가 line 1-71), `app/admin/codes/components/CodeGroupSection.tsx` (변경 line 11, line 19-20, line 35-36, line 174-181, line 207-212, line 225-230)
- **내용**: 코드 그룹 조회 Context 연결과 생성·수정·삭제 API 요청, 요청 중 로딩 및 오류 상태, 기존 성공·실패 알림을 전용 훅으로 이동했다. CodeGroupSection은 기존 JSX와 테이블 행 편집·폼 입력·선택 상태 및 DetailCodeSection 연결을 유지한 채 훅 반환값만 연결한다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. 요청 범위에 따라 별도 브라우저 수동 검증과 lint는 실행하지 않았다.

### 17:42 스트리머 관리 전용 훅 분리

- **대상**: `app/admin/streamers/hooks/useStreamerManagement.ts` (추가 line 1-31), `app/admin/streamers/page.tsx` (변경 line 1-23)
- **내용**: 스트리머 선택·등록·수정·저장·Chzzk 모달 열기/닫기와 채널 선택, 로딩·알림 상태를 전용 훅으로 이동했다. 기존 목록·폼·Chzzk 모달 UI와 API 계약은 유지했다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. 페이지에 상태·API 호출이 없고 훅 반환값이 목록·폼·Chzzk 모달 props에 연결된 것을 확인했다. ESLint 설정 파일이 없어 실행하지 않았고 새 설정은 추가하지 않았다.


### 17:39 맵 관리 전용 훅 분리

- **대상**: `app/admin/maps/hooks/useMapManagement.ts` (추가 line 1-90), `app/admin/maps/page.tsx` (변경 line 1-24), `app/admin/maps/components/MapListSection.tsx` (변경 line 5, line 15-48, line 50-56, line 140-185)
- **내용**: 맵 조회·검색·필터·CRUD·OverFast 일괄 등록·모달·알림 상태를 전용 훅으로 이동하고, 페이지와 목록 컴포넌트는 훅 반환값을 전달·표시하도록 축소했다. API·Context·다른 도메인 파일은 변경하지 않았다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. `maps/page.tsx`에 상태·API 호출이 없고 `useMapManagement` 반환값이 세 UI 컴포넌트 props에 연결된 것을 확인했다. ESLint 설정 파일이 없어 실행하지 않았고 새 설정은 추가하지 않았다.


### 17:33 맵 모드 공통코드 조회 중복 제거

- **대상**: `app/admin/maps/page.tsx` (변경 line 13, line 34-38, line 49-52)
- **내용**: 직접 관리하던 MAP_MODE API 조회·로딩 상태를 `useCommonCodes(MAP_MODE_GROUP_CODE)` 반환값으로 교체하고, 훅 오류를 기존 화면 알림으로 전달했다. 기존 맵 모드 필터와 저장 요청 흐름은 유지했다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. `rg -n '/api/codes\?groupCode' app/admin/maps/page.tsx` 결과 없음으로 중복 조회 제거를 확인했다. 브라우저 수동 검증은 실행하지 않았으며, 코드 경로 검토로 필터·저장 흐름 유지 여부를 확인했다.


### 14:10 스트리머 내부 명칭 전환 완료

- **대상**: `app/admin/streamers/page.tsx` (변경 line 1-32), `app/admin/streamers/components/StreamerListSection.tsx` (변경 line 1-274), `app/admin/streamers/components/StreamerFormSection.tsx` (변경 line 1-205), `app/admin/streamers/components/ChzzkSearchModal.tsx` (변경 line 1-302)
- **내용**: 스트리머 화면의 목록·폼·Chzzk 모달에서 Member 접두사와 members, selectedMemberId, 등록 상태 변수를 Streamer 기준으로 통일했다. `/admin/members` redirect와 Chzzk 외부 식별자는 유지했다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. 지정된 Member 식별자 검색 결과 없음. ESLint는 설정 파일이 없어 실행하지 않았다.

### 13:55 콘텐츠 출처 타입 의존성 분리

- **대상**: `lib/types/content.ts` (추가 line 1-6), `lib/types/heroes.ts` (변경 line 5), `lib/types/maps.ts` (변경 line 5)
- **내용**: 영웅과 맵의 DB·DTO·폼에서 공통으로 사용하는 `OVERFAST | MANUAL` 출처 값을 `ContentSource` 공통 타입으로 분리했다. 영웅 타입의 맵 타입 직접 의존성을 제거하고 기존 API·DB·화면 타입 의미를 유지했다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. `rg` 검색에서 `ContentSource` 선언은 공통 타입 파일 한 곳만 확인했다. ESLint는 설정 파일이 없어 실행하지 않았고, 타입 전용 변경으로 브라우저 검증은 불필요하다.

### 13:52 맵 모드 변환 중복 통합

- **대상**: `lib/constants/maps.ts` (변경 line 6-11), `lib/maps/mapClient.ts` (변경 line 1-22)
- **내용**: OverFast `gamemodes`를 `MAP_MODE` 공통코드 후보로 변환하는 `getOverFastModeCodes`의 입력을 읽기 전용 문자열 배열로 명시하고, 일괄 등록 요청의 인라인 정규화 로직을 해당 함수 호출로 통합했다. 화면 표시·선택·저장 요청 및 API 응답 형식은 유지했다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. `rg` 검색에서 인라인 정규화는 공통 함수 본문 한 곳만 확인했다. ESLint는 설정 파일이 없어 실행하지 않았고, 브라우저 검증은 자동화 도구와 인증 세션 부재로 미실행했다.

### 13:24 관리자 구조 개선 반영

- **대상**: `app/api/maps/import/route.ts` (변경 line 1-21), `app/api/heroes/import/route.ts` (변경 line 1-21), `lib/maps/mapService.ts` (변경 line 1-50), `lib/heroes/heroService.ts` (변경 line 1-80), `lib/maps/mapClient.ts` (변경 line 1-21), `lib/heroes/heroClient.ts` (변경 line 1-39), `lib/streamers/streamerClient.ts` (추가 line 1-14), `lib/codes/codeClient.ts` (추가 line 1-20)
- **내용**: 지도·영웅 일괄 등록의 검증과 트랜잭션을 도메인 서비스로 이동하고, 스트리머·공통코드 요청을 도메인 client로 통일했다. 기존 API 요청·응답 형식과 빈 배열 400 응답을 유지했다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. `npm.cmd run lint`는 ESLint 설정이 없어 초기 설정 프롬프트에서 중단되어 미실행 처리했다.

### 13:24 기능별 Context·코드 관리 모듈 분리

- **대상**: `lib/context/AdminContext.tsx` (변경 line 1-80), `lib/context/AdminFeatureContexts.tsx` (변경 line 1-167), `lib/hooks/useAdminMutation.ts` (변경 line 1-113), `components/layout/AdminHeader.tsx` (변경 line 1-161), `components/layout/AdminSidebar.tsx` (변경 line 1-348), `app/admin/page.tsx` (변경 line 1-473), `app/admin/members/page.tsx` (변경 line 1-184), `app/admin/codes/page.tsx` (변경 line 1-60), `app/admin/codes/components/CodeGroupSection.tsx` (변경 line 1-522), `app/admin/codes/components/DetailCodeSection.tsx` (변경 line 1-630), `app/admin/codes/components/codeFormState.ts` (추가 line 1-13), `app/admin/codes/components/codeTableColumns.tsx` (추가 line 1-30), `lib/types/admin.ts` (변경 line 1-3), `lib/types/tournaments.ts` (추가 line 1-26), `lib/mock/adminData.ts` (삭제)
- **내용**: 비토너먼트 소비처를 기능별 Context로 전환하고, 토너먼트 화면의 기존 import를 위해 최소 호환 상태만 남겼다. 공통코드 관리의 API 호출·폼 상태·테이블 컬럼을 하위 모듈로 분리했으며, 미사용 관리자 mock은 전체 사용처 확인 후 삭제했다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. `npm.cmd run lint`는 ESLint 설정이 없어 초기 설정 프롬프트에서 중단되어 미실행 처리했다. 토너먼트 경로는 이번 작업에서 수정하지 않았으며, 작업 전부터 존재한 `app/admin/tournaments/structure/page.tsx` 변경은 보존했다.

### 13:08 역할 공통코드 적용

- **대상**: `lib/constants/commonCodes.ts` (변경 line 1-7), `lib/hooks/useCommonCodes.ts` (변경 line 1-53), `lib/types/heroes.ts` (변경 line 7-8), `lib/types/admin.ts` (변경 line 56-57), `lib/heroes/heroValidator.ts` (변경 line 4-36), `app/api/heroes/route.ts` (변경 line 57, line 82), `app/api/heroes/import/route.ts` (변경 line 7, line 47-53), `app/admin/heroes/page.tsx` (변경 line 6, line 21-22, line 27-29, line 44, line 56-63, line 132-155, line 174, line 266-270, line 332, line 366, line 436-437, line 479), `app/admin/heroes/components/OverFastHeroModal.tsx` (변경 line 11, line 25, line 29, line 46-48), `app/admin/tournaments/structure/page.tsx` (변경 line 9-13, line 32-40, line 54-61, line 93-105, line 114, line 124, line 137, line 270, line 382, line 390, line 398, line 418-430, line 531-539, line 667-691)
- **내용**: HERO_ROLE과 PARTICIPANT_ROLE의 활성 코드만 조회하는 캐시형 `useCommonCodes`를 적용했다. 영웅 역할의 표시·등록·수정·필터·가져오기·서버 검증을 공통코드로 전환하고, 대회 참여 역할은 기존 한글 저장값을 유지해 등록·수정·검색·필터·표시를 공통코드 이름으로 구성했다. PLAYER_POSITION은 현재 화면/API 사용처가 없고 기존 Position enum의 HEALER와 새 코드가 달라 호환을 위해 변경하지 않았다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. 하드코딩 재검색으로 역할 옵션·라벨·검증 상수 제거를 확인했다. `npm.cmd run lint`는 ESLint 설정이 없어 초기 설정 선택 화면에서 중단되어 미실행 처리했다.

### 12:51 관리자 구조 개선 적용

- **대상**: `lib/types/streamers.ts` (변경 line 1-37), `lib/types/maps.ts` (변경 line 1-41), `lib/types/heroes.ts` (변경 line 1-27), `lib/types/codes.ts` (변경 line 1-20), `app/admin/members/page.tsx` (변경 line 12-17), `app/admin/maps/page.tsx` (변경 line 18-19), `app/admin/heroes/page.tsx` (변경 line 22), `app/admin/codes/components/CodeGroupSection.tsx` (변경 line 12), `app/admin/codes/components/DetailCodeSection.tsx` (변경 line 19), `lib/heroes/heroClient.ts` (변경 line 4), `lib/heroes/heroDto.ts` (변경 line 3), `lib/maps/mapClient.ts` (변경 line 4), `lib/maps/mapDto.ts` (변경 line 3)
- **내용**: 스트리머·맵·영웅·코드 타입을 기능별 파일로 분리하고, Prisma `Streamer` 모델과 기존 API 기준에 맞춰 비토너먼트 관리 영역의 타입 import를 전환했다. Chzzk는 스트리머의 선택 연동 채널로 유지했다.
- **검증**: `npx.cmd tsc --noEmit` 통과. `npm.cmd run lint`는 ESLint 설정이 없어 초기 설정 프롬프트에서 중단되어 미실행 처리했다.

### 12:51 스트리머·코드 그룹 API 계층 분리

- **대상**: `app/api/streamers/route.ts` (변경 line 1-68), `lib/streamers/chzzk.ts` (변경 line 1-10), `lib/streamers/streamerDto.ts` (변경 line 1-27), `lib/streamers/streamerValidator.ts` (변경 line 1-56), `lib/streamers/streamerService.ts` (변경 line 1-46), `app/api/codes/groups/route.ts` (변경 line 1-63), `lib/codes/codeGroupDto.ts` (변경 line 1-11), `lib/codes/codeGroupValidator.ts` (변경 line 1-17), `lib/codes/codeGroupService.ts` (변경 line 1-17)
- **내용**: 두 API route의 Prisma 직접 접근을 제거하고 DTO·validator·service로 역할을 분리했다. 기존 `/api/streamers`, `/api/codes/groups` 경로와 `{ success, data, message }` 성공 응답 형식은 유지했다.
- **검증**: `npx.cmd tsc --noEmit` 통과. `npm.cmd run lint`는 ESLint 초기 설정 프롬프트로 완료할 수 없었다.

### 12:51 페이지 범위 데이터 요청 및 미사용 공통 소스 정리

- **대상**: `lib/context/AdminFeatureContexts.tsx` (변경 line 24-94), `components/admin/AdminPageHeader.tsx` (삭제), `lib/hooks/useCommonCodes.ts` (삭제), `lib/constants/codes.ts` (삭제), `app/admin/members/components/ChzzkSearchModal.tsx` (변경 line 12-16), `app/admin/members/components/MemberListSection.tsx` (변경 line 7-8), `app/admin/members/components/MemberFormSection.tsx` (변경 line 12), `app/admin/maps/components/MapListSection.tsx` (변경 line 6-7), `app/admin/maps/components/MapFormSection.tsx` (변경 line 6-7), `app/admin/maps/components/OverFastMapModal.tsx` (변경 line 11), `app/admin/maps/components/mapForm.ts` (변경 line 5), `app/admin/heroes/components/OverFastHeroModal.tsx` (변경 line 11), `app/admin/maps/page.tsx` (변경 line 18-19)
- **내용**: 관리 레이아웃에 Provider는 유지하되 현재 URL이 필요한 도메인만 조회하도록 변경했고, 대시보드만 전체 요약 데이터를 조회한다. 참조가 없는 페이지 헤더·코드 훅과 이전 Chzzk 상수 파일은 안전하게 제거했다.
- **검증**: `npx.cmd tsc --noEmit` 통과, `git diff --check` 통과. `npm.cmd run lint`는 ESLint 설정 부재로 초기 설정 프롬프트에서 중단됐다. `git diff --name-only -- app/admin/tournaments/** app/api/tournaments/**` 결과가 비어 토너먼트 파일 변경이 없음을 확인했다.

### 13:10 코드 주석 정비

- **대상**: `lib/prisma.ts` (변경 line 1), `app/api/overwatch/maps/route.ts` (변경 line 1, line 7, line 10), `app/admin/members/components/MemberListSection.tsx` (변경 line 1-3, line 24), `app/admin/members/components/MemberFormSection.tsx` (변경 line 1-3, line 24)
- **내용**: 비토너먼트 영역의 Prisma 초기화, 외부 맵 조회 API, 회원 관리 컴포넌트에 파일 역할과 유지보수 경계 주석을 한글로 추가했다. 기존 사용자 변경 파일과 토너먼트 파일은 수정하지 않았다.
- **검증**: `git diff --check`, `cmd /c npx tsc --noEmit` 통과. `cmd /c npm run lint`는 기존 ESLint 설정 부재로 초기 설정 선택 화면에서 중단되어 실행하지 않았다.

### 02:57 관리자 인증 및 API 접근 제어 작업 정리

- **대상**: 관리자 로그인·권한·API 보호 관련 신규 파일과 관리자 화면/API 변경 파일 전체, `prisma/schema.prisma`, 관련 마이그레이션, `package.json`, 레거시 Supabase·타입 파일 삭제 (최종 파일 기준 변경 라인은 커밋 전 확인)
- **내용**: NextAuth 기반 로그인·권한 가드와 DEV 역할을 추가하고 관리자 데이터 API를 서비스·검증 모듈로 분리했다. 관리자 화면의 API 호출을 정리하고 기능별 Provider를 구성했으며, 토너먼트 소비자를 위해 기존 `useAdmin` 호환 상태를 유지했다. 사용하지 않는 Supabase·레거시 타입 파일은 제거했다.
- **검증**: `git status --short`, `git diff --stat`, `git diff`, 변경 파일의 토너먼트 참조 검색을 실행했다. `npx tsc --noEmit` 및 `npm run lint`는 이번 커밋 단계에서 재실행하지 않았다.

### 02:14 관리자 구조 점진 리팩터링

- **대상**: `lib/context/AdminContext.tsx` (변경 line 1-63), `lib/context/AdminFeatureContexts.tsx` (변경 line 1-94), `app/admin/maps/page.tsx` (변경 line 7-10, line 22-23, line 140-143, line 191-193), `app/admin/heroes/page.tsx` (변경 line 15-18, line 38-39, line 243, line 273), `lib/maps/mapClient.ts` (변경 line 1-16), `lib/heroes/heroClient.ts` (변경 line 1-18)
- **내용**: 회원·맵·영웅·코드 데이터와 피드백·화면 상태를 기능별 Provider로 분리하고 기존 `useAdmin` 호환 인터페이스를 유지했다. 지도·영웅 화면의 저장·일괄 등록 HTTP 호출을 기능 클라이언트로 분리했다.
- **검증**: `cmd /c npx tsc --noEmit` 통과. `npm run lint`는 ESLint 초기 설정 파일이 없어 대화형 설정 화면에서 중단되어 미실행으로 기록한다.

### 02:14 API와 회원 명칭 정리

- **대상**: `app/api/maps/route.ts` (변경 line 1-35), `lib/maps/mapValidator.ts` (변경 line 1-22), `lib/maps/mapService.ts` (변경 line 1-16), `app/api/heroes/route.ts` (변경 line 1-35), `lib/heroes/heroValidator.ts` (변경 line 1-14), `lib/heroes/heroService.ts` (변경 line 1-11), `app/api/codes/route.ts` (변경 line 1-42), `lib/codes/codeDto.ts` (변경 line 1-6), `lib/codes/codeValidator.ts` (변경 line 1-12), `lib/codes/codeService.ts` (변경 line 1-12), `app/api/members/route.ts` (변경 line 1-5), `app/admin/members/page.tsx` (변경 line 111, line 144)
- **내용**: maps·heroes·codes API의 검증과 Prisma 접근을 기능 모듈로 분리했다. 회원 화면을 `/api/members`로 전환하고 기존 `/api/streamers`는 호환 경로로 유지했다. 요청 메서드와 `{ success, data }` 응답 형식을 유지했다.
- **검증**: `cmd /c npx tsc --noEmit` 통과. `git diff -- app/admin/tournaments app/admin/tournaments/structure app/api/tournaments` 결과가 비어 토너먼트 관련 파일 미변경을 확인했다.

### 02:14 미사용 타입·래퍼 정리

- **대상**: `lib/types/database.ts` (삭제), `lib/supabase/client.ts` (삭제)
- **내용**: 프로젝트 전체 참조 검색에서 사용처가 없는 레거시 데이터베이스 타입과 Prisma 재노출 Supabase 래퍼를 제거했다.
- **검증**: `rg -n 'supabase/client|types/database'`로 참조가 없음을 확인하고 `cmd /c npx tsc --noEmit`을 통과했다.

### 12:22 스트리머 도메인명 복원

- **대상**: `lib/context/AdminFeatureContexts.tsx` (변경 line 65, line 73, line 77), `lib/context/AdminContext.tsx` (변경 line 20, line 26, line 31-33, line 63, line 108, line 118), `app/admin/members/page.tsx` (변경 line 111, line 144), `app/api/members/route.ts` (삭제)
- **내용**: 스트리머가 대회 참가 후보로 등록되는 실제 도메인 의미에 맞춰 임시 `/api/members` 경로를 제거하고, 기능 Provider와 화면 저장 요청을 기존 `/api/streamers`로 복원했다. 토너먼트 미구현 화면이 참조하는 기존 `members` 호환 필드는 유지했다.
- **검증**: `rg -n '/api/members|AdminMembersProvider|useAdminMembers' app lib -g '*.ts' -g '*.tsx'` 결과 없음, `cmd /c npx tsc --noEmit` 통과, `git diff --check` 통과. `npm run build`는 격리 환경의 `spawn EPERM`으로 미통과했다.
