# ROMS 작업 완료 규칙

## 개발일지 (필수 완료 단계)

코드, 설정, 문서, 데이터베이스 스키마 등 프로젝트 파일을 변경하는 작업은 최종 응답 직전에 반드시 아래 절차를 수행한다.

1. 구현과 검증이 끝난 뒤 `산출물/개발일지/ROMS_개발일지_YYYY-MM-DD.md`를 당일 파일로 생성하거나 갱신한다.
2. 이번 작업의 변경 사항마다 다음 정보를 빠짐없이 기록한다.
   - 수정·추가·삭제한 파일의 저장소 상대 경로
   - 최종 파일 기준 변경 코드의 줄 번호 또는 줄 범위
   - 변경 목적과 핵심 구현 내용
   - 실행한 검증 명령과 결과, 미실행 항목 및 이유
3. 같은 날짜의 일지 파일을 새로 만들었다면 `산출물/개발일지/ROMS_개발일지.md` 마스터 인덱스에도 항목을 추가하고 최종 수정일을 갱신한다.
4. 사용자가 만든 기존 변경 사항은 개발일지에 이번 작업의 변경으로 기록하지 않는다.
5. 최종 답변에는 개발일지 작성 완료 사실과 해당 파일 링크를 포함한다.
6. 최종 답변에는 이번 작업의 변경 파일 요약을 반드시 포함한다.
   - `수정`, `추가`, `삭제`를 구분한다.
   - 각 파일의 저장소 상대 경로와 최종 파일 기준 변경 코드의 줄 번호 또는 줄 범위를 적는다.
   - 각 항목의 핵심 변경 내용을 한 줄로 요약한다.

개발일지의 Notion 업로드(`npm run log:notion`)는 외부 시스템 변경이므로 사용자가 명시적으로 요청한 경우에만 실행한다.

## 코드 주석 규칙

1. 페이지 파일과 신규 컴포넌트 파일의 최상단에는 파일의 목적을 설명하는 한 줄 주석을 작성한다.
2. 새로 작성하거나 의미 있게 수정하는 함수와 React 컴포넌트 바로 위에는 역할, 주요 입력 또는 처리 결과를 설명하는 짧은 주석을 작성한다.
3. 자명한 JSX·단순 상태 변경을 줄마다 설명하지 말고, 유지보수 시 의도를 알기 어려운 경계와 동작에만 주석을 둔다.
4. 코드 주석은 한글로 작성한다. 파일 경로, 컴포넌트명, API명, 코드 식별자처럼 고유명사가 필요한 경우에만 원문 표기를 허용한다.
## 추가 작업 기록 규칙

### 최종 응답의 변경 라인 표기

1. 변경 라인은 최종 파일 기준으로 기록한다.
2. 한 줄은 `line 7`, 연속된 여러 줄은 반드시 `line 7-9` 형식으로 기록한다.
3. 비연속 라인은 `line 7-9, line 14-16`처럼 각각 범위를 분리한다.

### 개발일지 작성 형식

1. 당일 개발일지의 기존 Development Journal/Timeline 영역에 시간순으로 추가한다.
2. 각 항목은 반드시 `### HH:MM 작업 제목` 형식으로 시작한다.
3. 각 항목은 아래 필드를 포함한다.

```markdown
### HH:MM 작업 제목

- **대상**: `repo/relative/path` (변경 line 7-9)
- **내용**: 이번 작업의 변경 목적과 핵심 구현 내용.
- **검증**: 실행한 명령과 결과, 미실행 항목 및 이유.
```

4. 별도 번호 섹션·임의의 표만 추가하지 말고 위 시간·제목·대상·내용·검증 형식을 우선한다.

### 파일 및 코드 주석 형식

1. 페이지와 신규 컴포넌트 파일의 최상단에는 아래 3개 정보를 순서대로 기록한다.
   - `File`: 저장소 상대 경로
   - `Page/Component`: 페이지 또는 컴포넌트 이름
   - `Purpose`: 페이지·컴포넌트의 역할을 1~2줄로 설명
2. 새 함수, React 컴포넌트, 또는 의미 있게 수정한 기능 코드 바로 위에는 역할과 주요 입력·처리 결과를 설명하는 한 줄 주석을 작성한다.
3. 단순 JSX·상태 대입에는 주석을 반복하지 않고, 유지보수에 필요한 경계·검증·외부 연동 동작에만 작성한다.
